import { createAtom, useAtom } from './use-atom';
var streamAtom = createAtom();
export function useStreamState() {
    var _a = useAtom(streamAtom), stream = _a[0], setStream = _a[1];
    return [stream, setStream];
}
