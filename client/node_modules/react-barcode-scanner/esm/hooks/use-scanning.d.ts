import { type RefObject } from 'react';
import { BarcodeFormat, type DetectedBarcode } from '../types';
export interface ScanOptions {
    delay?: number;
    formats?: Array<BarcodeFormat | string>;
}
export declare function useScanning(ref: RefObject<HTMLVideoElement>, provideOptions?: ScanOptions): {
    detectedBarcodes: DetectedBarcode[] | undefined;
    startScan: () => void;
    stopScan: () => void;
};
