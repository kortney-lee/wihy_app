import React from 'react';
// import VHealthSearch from '../VHealthSearch'; // Commented out - not needed in user app

const App: React.FC = () => {
  return (
    <div>
      <h1>User App Components</h1>
      <p>This component is not currently used in the user dashboard.</p>
    </div>
  );
};

export default App;