name: Build and Push to Azure Container Registry

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  REGISTRY_LOGIN_SERVER: wihymlregistry-b6fdh5cmhzgwbbgy.azurecr.io
  IMAGE_NAME: wihy-ui

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install root dependencies
      run: npm ci
    
    - name: Install client dependencies
      run: |
        cd client
        npm ci
    
    - name: Run client tests
      run: |
        cd client
        npm test -- --coverage --watchAll=false
    
    - name: Build client
      run: |
        cd client
        npm run build

  build-and-push-azure:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Check Azure credentials
      run: |
        echo "🔑 Checking Azure credentials..."
        
        # Check individual secrets
        if [ -z "${{ secrets.AZURE_CLIENT_ID }}" ]; then
          echo "❌ AZURE_CLIENT_ID secret not configured"
          MISSING=true
        else
          echo "✅ AZURE_CLIENT_ID configured"
        fi
        
        if [ -z "${{ secrets.AZURE_TENANT_ID }}" ]; then
          echo "❌ AZURE_TENANT_ID secret not configured"  
          MISSING=true
        else
          echo "✅ AZURE_TENANT_ID configured"
        fi
        
        if [ -z "${{ secrets.AZURE_CREDENTIALS }}" ]; then
          echo "❌ AZURE_CREDENTIALS secret not configured"
          MISSING=true
        else
          echo "✅ AZURE_CREDENTIALS configured"
        fi
        
        if [ "$MISSING" = true ]; then
          echo ""
          echo "⚠️ Required Azure secrets are missing!"
          echo "Please configure the following in repository Settings → Secrets:"
          echo "- AZURE_CLIENT_ID"
          echo "- AZURE_TENANT_ID" 
          echo "- AZURE_CREDENTIALS"
          echo ""
          echo "See docs/AZURE_GITHUB_ACTIONS_SETUP.md for setup instructions"
          exit 1
        fi
        
        echo "✅ All Azure credentials configured"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Log in to Azure Container Registry
      run: |
        echo "� Logging into wihymlregistry..."
        az acr login --name wihymlregistry

    - name: Build and push Docker image
      run: |
        echo "🏗️ Building with wihymlregistry..."
        echo "Registry: wihymlregistry"
        echo "Image: ${{ env.IMAGE_NAME }}"
        echo "SHA: ${{ github.sha }}"
        
        # Test ACR connectivity first
        echo "Testing ACR connectivity..."
        az acr show --name wihymlregistry --query "loginServer" -o tsv || {
          echo "❌ Cannot access ACR. Check permissions."
          exit 1
        }
        
        # Build and push with error handling
        az acr build \
          --registry wihymlregistry \
          --image ${{ env.IMAGE_NAME }}:${{ github.sha }} \
          --image ${{ env.IMAGE_NAME }}:latest \
          --timeout 1800 \
          --verbose \
          . || {
          echo "❌ ACR build failed. Error details above."
          echo "Common causes:"
          echo "1. Azure credentials expired or incorrect"
          echo "2. ACR permissions insufficient"
          echo "3. Build timeout or resource limits"
          exit 1
        }

    - name: Image Info
      run: |
        echo "🐳 Container images published to Azure Container Registry:"
        echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
        echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
        echo ""
        echo "✅ Ready for Azure Container Apps deployment!"